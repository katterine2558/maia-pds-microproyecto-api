# Credenciales del remoto DVC (bucket S3 del proyecto).
# Copiar a .env y pegar las llaves que comparte el equipo por fuera del repo.
# .env esta en .gitignore. Nunca commitearlo.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
